{{!--
  This partial renders information specific to an enum.
  @param {object} member - The enum model to render information for.
  @param {string} level - The heading level to use for subheadings.
--}}
{{#if member.metadata.explicitCustomAttributes}}
Attributes: {{>attribute_list member.metadata.explicitCustomAttributes}}
{{/if}}

Inheritance: {{>inheritance_list type=member.metadata}}

{{#if model.values}}
{{{level}}} Fields
| Name | Value | Description |
|------|:-----:|-------------|
{{#each model.values}}
| {{name}} | {{metadata.literalValue}} | {{doc.summary}} |
{{/each}}
{{/if}}